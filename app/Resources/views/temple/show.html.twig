{% extends 'base.html.twig' %}

{% block body %}
    <h1>Temple</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temple.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ temple.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ temple.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('temple_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('temple_edit', { 'id': temple.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
