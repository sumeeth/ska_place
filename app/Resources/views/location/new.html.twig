{% extends 'base.html.twig' %}

{% block body %}
    <h1>Location creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn" href="{{ path('location_index') }}" role="button">Back to the list</a>
        </li>
    </ul>
{% endblock %}
